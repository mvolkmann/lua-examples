-- I found the source for the file strict.lua online and
-- copied it to ~/lua which is at the beginning of package.path
-- due to adding the following line in ~/.zshrc.
-- export LUA_PATH="${HOME}/lua/?.lua;;"
--
-- Run this code with "lua -l strict strict-demo.lua" to use strict mode.
foo = 3
